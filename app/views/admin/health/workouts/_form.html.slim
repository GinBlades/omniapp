= simple_form_for([:admin,@health_workout]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :health_workout_category
    = f.input :start, as: :string, input_html: { type: 'datetime-local', value: time_form(@health_workout.start) || time_form(Time.now) } 
    = f.input :duration, label: "Duration (in minutes)"
    = f.input :distance
    = f.input :rating, as: :radio_buttons, collection: 1..10
    = f.input :notes, as: :ckeditor

  .form-actions
    = f.button :submit, class: 'btn btn-success'
