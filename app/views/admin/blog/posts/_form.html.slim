= simple_form_for([:admin,@blog_post]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :user
    = f.input :title
    = f.input :slug
    = f.input :excerpt
    = f.input :body
    = f.input :published_at, as: :string, input_html: { type: 'datetime-local', value: time_form(@blog_post.published_at) || time_form(Time.now) } 

  .form-actions
    = f.button :submit, class: 'btn btn-success'
